Spacy is able to detect dates and work of art. However, it is unable to detect something like just `Celtics` but is able to detect `Boston Celtics`.

```bash
pip install spacy
python -m spacy download en_core_web_sm
```
